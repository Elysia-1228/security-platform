{
  "name": "zhilian-project-runner",
  "version": "1.0.0",
  "description": "Unified runner for Zhilian2025 project",
  "scripts": {
    "setup": "npm install && pip install -r PythonIDS/requirements.txt && cd FrontCode && npm install",
    "start": "concurrently --kill-others-on-fail --prefix \"[{name}]\" --names \"FRONT,BACK,IDS\" --prefix-colors \"blue,magenta,green\" \"npm run start:front\" \"npm run start:back\" \"npm run start:ids\"",
    "start:front": "cd FrontCode && npm install && npm run dev",
    "start:back": "cd BackCode && mvn spring-boot:run",
    "start:ids": "python PythonIDS/anomaly_based_ids/realtime_detection_fixed.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
